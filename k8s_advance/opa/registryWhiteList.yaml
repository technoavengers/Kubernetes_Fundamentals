apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sRegistryWhiteList
metadata:
  name: allowed-registry
spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Pod"]
  parameters:
    registry: ["docker.io"]
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-routing
spec:
  rules:
    - host: vehicle.technoavengers.click
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service: 
                name: fleetman-webapp
                port:
                 number: 80
